# Staging Cluster Configuration for Progressive Rollout Demo
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: rollout-staging
networking:
  apiServerAddress: "127.0.0.1"
  apiServerPort: 42445
  disableDefaultCNI: false
  podSubnet: "10.252.0.0/16"
  serviceSubnet: "10.102.0.0/12"
nodes:
- role: control-plane
  kubeadmConfigPatches:
  - |
    kind: InitConfiguration
    nodeRegistration:
      kubeletExtraArgs:
        node-labels: "ingress-ready=true,environment=staging"
        authorization-mode: "Webhook"
    ---
    kind: KubeletConfiguration
    serverTLSBootstrap: true
  - |
    kind: ClusterConfiguration
    metadata:
      name: config
    apiServer:
      extraArgs:
        runtime-config: "api/all=true"
        feature-gates: "EphemeralContainers=true"
    controllerManager:
      extraArgs:
        feature-gates: "EphemeralContainers=true"
        bind-address: "0.0.0.0"
    scheduler:
      extraArgs:
        feature-gates: "EphemeralContainers=true"
        bind-address: "0.0.0.0"
  extraPortMappings:
  - containerPort: 30080
    hostPort: 30982
    protocol: TCP
  - containerPort: 30443
    hostPort: 30985
    protocol: TCP
- role: worker
  kubeadmConfigPatches:
  - |
    kind: JoinConfiguration
    nodeRegistration:
      kubeletExtraArgs:
        node-labels: "environment=staging,workload-type=integration"