/*
Copyright The kcp Authors.

Licensed under the Apache License, Version 2.0 (the "License");
you may not use this file except in compliance with the License.
You may obtain a copy of the License at

    http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing, software
distributed under the License is distributed on an "AS IS" BASIS,
WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
See the License for the specific language governing permissions and
limitations under the License.
*/

// Code generated by cluster-lister-gen. DO NOT EDIT.

package v1alpha2

import (
	schedulingv1alpha2 "k8s.io/api/scheduling/v1alpha2"
	"k8s.io/apimachinery/pkg/labels"
	listersschedulingv1alpha2 "k8s.io/client-go/listers/scheduling/v1alpha2"
	"k8s.io/client-go/tools/cache"

	kcplisters "github.com/kcp-dev/client-go/third_party/k8s.io/client-go/listers"
	"github.com/kcp-dev/logicalcluster/v3"
)

// PodGroupClusterLister helps list PodGroups across all workspaces,
// or scope down to a PodGroupLister for one workspace.
// All objects returned here must be treated as read-only.
type PodGroupClusterLister interface {
	// List lists all PodGroups in the indexer.
	// Objects returned here must be treated as read-only.
	List(selector labels.Selector) (ret []*schedulingv1alpha2.PodGroup, err error)
	// Cluster returns a lister that can list and get PodGroups in one workspace.
	Cluster(clusterName logicalcluster.Name) listersschedulingv1alpha2.PodGroupLister
	PodGroupClusterListerExpansion
}

// podGroupClusterLister implements the PodGroupClusterLister interface.
type podGroupClusterLister struct {
	kcplisters.ResourceClusterIndexer[*schedulingv1alpha2.PodGroup]
}

var _ PodGroupClusterLister = new(podGroupClusterLister)

// NewPodGroupClusterLister returns a new PodGroupClusterLister.
// We assume that the indexer:
// - is fed by a cross-workspace LIST+WATCH
// - uses kcpcache.MetaClusterNamespaceKeyFunc as the key function
// - has the kcpcache.ClusterIndex as an index
// - has the kcpcache.ClusterAndNamespaceIndex as an index
func NewPodGroupClusterLister(indexer cache.Indexer) PodGroupClusterLister {
	return &podGroupClusterLister{
		kcplisters.NewCluster[*schedulingv1alpha2.PodGroup](indexer, schedulingv1alpha2.Resource("podgroup")),
	}
}

// Cluster scopes the lister to one workspace, allowing users to list and get PodGroups.
func (l *podGroupClusterLister) Cluster(clusterName logicalcluster.Name) listersschedulingv1alpha2.PodGroupLister {
	return &podGroupLister{
		l.ResourceClusterIndexer.WithCluster(clusterName),
	}
}

// podGroupLister can list all PodGroups inside a workspace
// or scope down to a listersschedulingv1alpha2.PodGroupNamespaceLister for one namespace.
type podGroupLister struct {
	kcplisters.ResourceIndexer[*schedulingv1alpha2.PodGroup]
}

var _ listersschedulingv1alpha2.PodGroupLister = new(podGroupLister)

// PodGroups returns an object that can list and get PodGroups in one namespace.
func (l *podGroupLister) PodGroups(namespace string) listersschedulingv1alpha2.PodGroupNamespaceLister {
	return &podGroupNamespaceLister{
		l.ResourceIndexer.WithNamespace(namespace),
	}
}

// podGroupNamespaceLister implements the listersschedulingv1alpha2.PodGroupNamespaceLister
// interface.
type podGroupNamespaceLister struct {
	kcplisters.ResourceIndexer[*schedulingv1alpha2.PodGroup]
}

var _ listersschedulingv1alpha2.PodGroupNamespaceLister = new(podGroupNamespaceLister)

// NewPodGroupLister returns a new PodGroupLister.
// We assume that the indexer:
// - is fed by a cross-workspace LIST+WATCH
// - uses kcpcache.MetaClusterNamespaceKeyFunc as the key function
// - has the kcpcache.ClusterIndex as an index
// - has the kcpcache.ClusterAndNamespaceIndex as an index
func NewPodGroupLister(indexer cache.Indexer) listersschedulingv1alpha2.PodGroupLister {
	return &podGroupLister{
		kcplisters.New[*schedulingv1alpha2.PodGroup](indexer, schedulingv1alpha2.Resource("podgroup")),
	}
}

// podGroupScopedLister can list all PodGroups inside a workspace
// or scope down to a listersschedulingv1alpha2.PodGroupNamespaceLister for one namespace.
type podGroupScopedLister struct {
	kcplisters.ResourceIndexer[*schedulingv1alpha2.PodGroup]
}

// PodGroups returns an object that can list and get PodGroups in one namespace.
func (l *podGroupScopedLister) PodGroups(namespace string) listersschedulingv1alpha2.PodGroupLister {
	return &podGroupLister{
		l.ResourceIndexer.WithNamespace(namespace),
	}
}
