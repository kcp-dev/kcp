apiVersion: v1
kind: Secret
metadata:
  name: kube-bind-controller-secret
  namespace: default
  annotations:
    bootstrap.kcp.io/create-only: "true"
    kubernetes.io/service-account.name: kube-bind-controller
type: kubernetes.io/service-account-token