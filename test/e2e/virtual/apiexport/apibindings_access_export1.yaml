apiVersion: apis.kcp.io/v1alpha2
kind: APIExport
metadata:
  name: export1
spec:
  resources:
  - name: widgets
    group: example.io
    schema: v1.widgets.example.io
    storage:
      crd: {}
  permissionClaims:
  - group: apis.kcp.io
    resource: apibindings
    verbs:
    - "*"
