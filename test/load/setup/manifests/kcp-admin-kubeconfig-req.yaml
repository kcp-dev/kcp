apiVersion: operator.kcp.io/v1alpha1
kind: Kubeconfig
metadata:
  name: kcp-admin
  namespace: kcp
spec:
  username: kcp-admin
  groups:
    - system:kcp:admin
  validity: 8766h
  secretRef:
    name: kubeconfig-kcp-admin
  target:
    frontProxyRef:
      name: frontproxy
