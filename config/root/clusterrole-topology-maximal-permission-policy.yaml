apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: system:kcp:apiexport:topology:maximal-permission-policy
rules:
- apiGroups: ["topology.kcp.io"]
  verbs: ["*"]
  resources:
  - partitions
  - partitionsets
