apiVersion: tenancy.kcp.dev/v1alpha1
kind: ClusterWorkspaceType
metadata:
  name: homeroot
spec:
  initializer: true
  defaultChildWorkspaceType:
    name: Homebucket
    path: root
  allowedChildren:
    types:
    - name: Homebucket
      path: root
  allowedParents:
    types:
    - name: Root
      path: root
