
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.7.0
  creationTimestamp: null
  name: locationdomains.scheduling.kcp.dev
spec:
  group: scheduling.kcp.dev
  names:
    categories:
    - kcp
    kind: LocationDomain
    listKind: LocationDomainList
    plural: locationdomains
    singular: locationdomain
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - description: Type of the workspace
      jsonPath: .spec.type
      name: Type
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: "LocationDomain defines a set of locations of a certain type
          sharing uniform properties of some kind. \n In case of Workload locations
          this includes - a common set of supported APIs - a common software defined
          networking layer for communication between the locations. - ability to reschedule
          workloads to a new location is few or no disruption of a workload."
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: LocationDomainSpec holds the desired state of the location
              domain.
            properties:
              instances:
                description: instances is a references to instance objects subject
                  to this location domain. Depending on type this will usually be
                  a workspace reference.
                properties:
                  resource:
                    description: resource is the group version resource of the instances
                      that are subject to this location domain. The creator of this
                      location domain needs to have verb "create" permission in the
                      referenced workspace on the given resource with the subresource
                      "locationdomain".
                    properties:
                      group:
                        description: group is the name of an API group.
                        pattern: ^(|[a-z0-9]([-a-z0-9]*[a-z0-9](\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*)?)$
                        type: string
                      resource:
                        description: resource is the name of the resource.
                        pattern: ^[a-z]([-a-z0-9]*[a-z0-9]$
                        type: string
                      version:
                        description: version is the version of the API.
                        pattern: ^[a-z]([-a-z0-9]*[a-z0-9]$
                        type: string
                    required:
                    - resource
                    - version
                    type: object
                  workspace:
                    description: workspace is a reference to a workspace with instances
                      subject to the location domain. Exactly one must be set.
                    properties:
                      name:
                        description: name is a workspace name meaning a workspace
                          defined in the workspace this LocationDomain is defined
                          it.
                        pattern: ^[a-z][a-z0-9-]*[a-z0-9]$
                        type: string
                    required:
                    - name
                    type: object
                required:
                - resource
                type: object
              locations:
                description: locations
                items:
                  properties:
                    availableSelectorLabels:
                      description: availableSelectorLabels is a list of labels that
                        can be used to select an instance at this location in a placement
                        object.
                      items:
                        description: AvailableSelectorLabel specifies a label with
                          key name and possible values.
                        properties:
                          description:
                            description: description is a human readable description
                              of the label.
                            type: string
                          key:
                            description: key is the name of the label.
                            maxLength: 255
                            pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9](\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*)?/)?([a-zA-Z0-9][-a-zA-Z0-9_.]{0,61})?[a-zA-Z0-9]$
                            type: string
                          values:
                            description: values are the possible values for this labels.
                            items:
                              description: LabelValue specifies a value of a label.
                              maxLength: 63
                              pattern: ^(|([a-z0-9]([-a-z0-9]*[a-z0-9](\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*)?/)?([a-zA-Z0-9][-a-zA-Z0-9_.]{0,61})?[a-zA-Z0-9])$
                              type: string
                            minItems: 1
                            type: array
                            x-kubernetes-list-type: set
                        required:
                        - key
                        - values
                        type: object
                      type: array
                      x-kubernetes-list-map-keys:
                      - key
                      x-kubernetes-list-type: map
                    description:
                      description: description is a human-readable description of
                        the location.
                      type: string
                    labelSelector:
                      description: "labelSelector chooses the instances that will
                        be part of this location. \n Note that these labels are not
                        what is shown in the Location objects to the user. Depending
                        on context, both will match or won't match."
                      properties:
                        matchExpressions:
                          description: matchExpressions is a list of label selector
                            requirements. The requirements are ANDed.
                          items:
                            description: A label selector requirement is a selector
                              that contains values, a key, and an operator that relates
                              the key and values.
                            properties:
                              key:
                                description: key is the label key that the selector
                                  applies to.
                                type: string
                              operator:
                                description: operator represents a key's relationship
                                  to a set of values. Valid operators are In, NotIn,
                                  Exists and DoesNotExist.
                                type: string
                              values:
                                description: values is an array of string values.
                                  If the operator is In or NotIn, the values array
                                  must be non-empty. If the operator is Exists or
                                  DoesNotExist, the values array must be empty. This
                                  array is replaced during a strategic merge patch.
                                items:
                                  type: string
                                type: array
                            required:
                            - key
                            - operator
                            type: object
                          type: array
                        matchLabels:
                          additionalProperties:
                            type: string
                          description: matchLabels is a map of {key,value} pairs.
                            A single {key,value} in the matchLabels map is equivalent
                            to an element of matchExpressions, whose key field is
                            "key", the operator is "In", and the values array contains
                            only "value". The requirements are ANDed.
                          type: object
                      type: object
                    labels:
                      additionalProperties:
                        description: LabelValue specifies a value of a label.
                        maxLength: 63
                        pattern: ^(|([a-z0-9]([-a-z0-9]*[a-z0-9](\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*)?/)?([a-zA-Z0-9][-a-zA-Z0-9_.]{0,61})?[a-zA-Z0-9])$
                        type: string
                      description: labels is a set of labels that all instances in
                        this location have in common.
                      type: object
                      x-kubernetes-map-type: atomic
                    name:
                      description: name is the name of the location.
                      minLength: 1
                      pattern: ^[a-z][a-z0-9-]*[a-z0-9]$
                      type: string
                  required:
                  - name
                  type: object
                type: array
              type:
                description: type defines which class of objects this location can
                  schedule. A typical type is "Workloads". The type determines which
                  scheduler is doing the actual scheduling tasks.
                type: string
              workspaceSelector:
                description: workspaceSelector defines which workspaces (labels) this
                  domain automatically is assigned to.
                properties:
                  labels:
                    description: labels are used to filter workspaces.
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector
                          requirements. The requirements are ANDed.
                        items:
                          description: A label selector requirement is a selector
                            that contains values, a key, and an operator that relates
                            the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: operator represents a key's relationship
                                to a set of values. Valid operators are In, NotIn,
                                Exists and DoesNotExist.
                              type: string
                            values:
                              description: values is an array of string values. If
                                the operator is In or NotIn, the values array must
                                be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced
                                during a strategic merge patch.
                              items:
                                type: string
                              type: array
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: matchLabels is a map of {key,value} pairs. A
                          single {key,value} in the matchLabels map is equivalent
                          to an element of matchExpressions, whose key field is "key",
                          the operator is "In", and the values array contains only
                          "value". The requirements are ANDed.
                        type: object
                    type: object
                  priority:
                    description: priority is used when multiple LocationDomains and
                      their workspace selector match. The largest number wins.
                    format: int32
                    type: integer
                type: object
            required:
            - instances
            type: object
          status:
            description: LocationDomainStatus defines the observed state of Location.
            properties:
              conditions:
                description: conditions is a list of conditions that apply to the
                  LocationDomain.
                items:
                  description: Condition defines an observation of a object operational
                    state.
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another. This should be when the underlying condition changed.
                        If that is not known, then using the time when the API field
                        changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: A human readable message indicating details about
                        the transition. This field may be empty.
                      type: string
                    reason:
                      description: The reason for the condition's last transition
                        in CamelCase. The specific API may choose whether or not this
                        field is considered a guaranteed API. This field may not be
                        empty.
                      type: string
                    severity:
                      description: Severity provides an explicit classification of
                        Reason code, so the users or machines can immediately understand
                        the current situation and act accordingly. The Severity field
                        MUST be set only when Status=False.
                      type: string
                    status:
                      description: Status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: Type of condition in CamelCase or in foo.example.com/CamelCase.
                        Many .condition.type values are consistent across resources
                        like Available, but because arbitrary conditions can be useful
                        (see .node.status.conditions), the ability to deconflict is
                        important.
                      type: string
                  required:
                  - lastTransitionTime
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
