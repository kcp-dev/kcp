
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.7.0
  creationTimestamp: null
  name: locations.scheduling.kcp.dev
spec:
  group: scheduling.kcp.dev
  names:
    categories:
    - kcp
    kind: Location
    listKind: LocationList
    plural: locations
    singular: location
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - description: Type of the workspace
      jsonPath: .spec.type
      name: Type
      type: string
    - description: Available instances in this location
      jsonPath: .status.availableInstances
      name: Available
      type: string
    - description: Instances in this location
      jsonPath: .status.instances
      name: Instances
      type: string
    - description: The common labels of this location
      jsonPath: .metadata.annotation."scheduling.kcp.dev/labels"
      name: Labels
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: "Location represents a scheduling target for instances of a given
          type. A location is a projection and aggregation of instances referenced
          in a LocationDomain, used to express scheduling choices by a user. A location
          can subsume a number of instances. \n The location is chosen by the user
          (in the future) through a Placement object, while the instance is chosen
          by the scheduler depending on considerations like load or available resources,
          or further node selectors specified by the user."
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: LocationSpec holds the desired state of the Location.
            properties:
              availableSelectorLabels:
                description: availableSelectorLabels is a list of labels that can
                  be used to select an instance at this location in a placement object.
                items:
                  description: AvailableSelectorLabel specifies a label with key name
                    and possible values.
                  properties:
                    description:
                      description: description is a human readable description of
                        the label.
                      type: string
                    key:
                      description: key is the name of the label.
                      maxLength: 255
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9](\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*)?/)?([a-zA-Z0-9][-a-zA-Z0-9_.]{0,61})?[a-zA-Z0-9]$
                      type: string
                    values:
                      description: values are the possible values for this labels.
                      items:
                        description: LabelValue specifies a value of a label.
                        maxLength: 63
                        pattern: ^(|([a-z0-9]([-a-z0-9]*[a-z0-9](\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*)?/)?([a-zA-Z0-9][-a-zA-Z0-9_.]{0,61})?[a-zA-Z0-9])$
                        type: string
                      minItems: 1
                      type: array
                      x-kubernetes-list-type: set
                  required:
                  - key
                  - values
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - key
                x-kubernetes-list-type: map
              description:
                description: description is a human-readable description of the location.
                type: string
              domain:
                description: domain is the LocationDomain this location belongs to.
                properties:
                  name:
                    description: name is the name of the location domain this location
                      belongs to.
                    minLength: 1
                    type: string
                  workspace:
                    description: workspace is the absolute workspace name (e.g. root:company)
                      of the domain this location belongs to.
                    pattern: ^[a-z][a-z0-9-]*[a-z0-9](:[a-z][a-z0-9-]*[a-z0-9])*$
                    type: string
                required:
                - name
                - workspace
                type: object
              type:
                description: type defines which class of objects this location can
                  schedule. A typical type is "Workloads".
                minLength: 1
                type: string
            required:
            - domain
            type: object
          status:
            description: LocationStatus defines the observed state of Location.
            properties:
              availableInstances:
                description: available is the number of actual instances that are
                  available at this location.
                format: int32
                type: integer
              instances:
                description: instances is the number of actual instances at this location.
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
