
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.7.0
  creationTimestamp: null
  name: serviceexports.apis.kcp.dev
spec:
  group: apis.kcp.dev
  names:
    categories:
    - kcp
    kind: ServiceExport
    listKind: ServiceExportList
    plural: serviceexports
    singular: serviceexport
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ServiceExport registers an API and implementation to allow consumption
          by others.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: Spec holds the desired state.
            properties:
              resourceSchemas:
                description: ResourceSchemas records which resource schemas are exposed
                  with this ServiceExport.
                items:
                  description: ResourceSchemaReference records a resource, identified
                    by (group, version, resource, schema).
                  properties:
                    group:
                      type: string
                    hash:
                      description: Hash is
                      type: string
                    resource:
                      type: string
                    schema:
                      description: Schema is the name of the bound ResourceSchema.
                      type: string
                    version:
                      type: string
                  required:
                  - group
                  - resource
                  - version
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - schema
                x-kubernetes-list-type: map
            type: object
          status:
            description: Status communicates the observed state.
            properties:
              resourceSchemasInUse:
                description: ResourceSchemasInUse records which schemas are actually
                  in use (that is, object exist at that schema in some Workspace that
                  binds this ServiceExport).
                items:
                  type: string
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
